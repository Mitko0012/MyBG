@model ViewEditsModel


@{
	if(Model.Edits.Count > 0)
	{
		<a asp-action="PageViewer" asp-controller="Page" asp-route-id="@Model.Edits[0].PageToEdit.Id">Back</a>
	}
	foreach (EditModel model1 in Model.Edits)
	{
		<div class="editDiv">
			<h3>Edit #@model1.PageIndex for @model1.PageToEdit.Title</h3>
			<a class="edit-redirect" asp-action="ViewEdit" asp-route-pageId="@model1.PageToEdit.Id" asp-route-editIndex="@model1.PageIndex">View edit</a>
		</div>
	}
}
 