@model ViewEditsModel

@{
	foreach (EditModel model1 in Model.Edits)
	{
		<div class="editDiv">
			<h3>Submission for @model1.PageToEdit.Title</h3>
			<a class="submit-a" asp-action="EditSubmissionAdmin" asp-route-id="@model1.ID">View submission</a>
		</div>
	}
}
